{
  "name": "casinotro",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "jsdom": "^30.0.1",
    "sass": "^1.102.0",
    "typescript": "^7.0.2",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
